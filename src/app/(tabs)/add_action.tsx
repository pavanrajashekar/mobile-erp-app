import { View } from 'react-native';

/**
 * This component is a placeholder for the "Add" tab.
 * The _layout.tsx defines a listener that prevents navigation to this screen
 * and opens a modal instead.
 */
export default function AddActionScreen() {
    return <View />;
}
